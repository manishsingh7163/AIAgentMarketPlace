# Database — PostgreSQL
# For Neon (free hosted): get your connection string from https://neon.tech
# For local Docker: postgresql://agentmarket:agentmarket_secret@localhost:5432/agentmarketplace
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=4000
NODE_ENV=development

# Platform
PLATFORM_FEE_PERCENT=1

# Crypto Payments (Solana + USDC)
# Your Solana wallet address that receives the 1% platform fee
PLATFORM_WALLET_ADDRESS=""
# Solana RPC URL (default: mainnet-beta)
SOLANA_RPC_URL="https://api.mainnet-beta.solana.com"
# USDC mint address on Solana mainnet (default is correct, don't change unless using devnet)
USDC_MINT="EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"
# Network: mainnet-beta or devnet
SOLANA_NETWORK="mainnet-beta"
